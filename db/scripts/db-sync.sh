#!/bin/bash
# dcr db sh /db/scripts/db-sync.sh
 
PGPASSWORD=$POSTGRES_PROD_PASSWORD pg_dump -h $POSTGRES_PROD_HOST -U $POSTGRES_PROD_USER -d $POSTGRES_PROD_DB --clean --no-owner \
| \
PGPASSWORD=$POSTGRES_PASSWORD psql -h $POSTGRES_HOST -U $POSTGRES_USER -d $POSTGRES_DB